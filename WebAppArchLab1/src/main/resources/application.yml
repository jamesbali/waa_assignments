


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/WAA_LabOne?useSSL=false&serverTimezone=UTC
    username: root
    password: Capinski@MySQL1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    root: INFO
    org:
      springframework: INFO

